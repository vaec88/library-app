## Database
- A Postgresql database on localhost or Supabase

## Backend
- Enter the database connection env var values (`DB_URL`, `DB_USERNAME`, `DB_PASSWORD`) ​​and the frontend URL (`FRONT_URL`)
on the src/main/resources/application.yaml file

## Frontend
- npm install
- ng serve